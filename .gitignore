node_modules/
dist/
ledger.jsonl
*.log

# Attestation core: the attestor's PRIVATE key must never be committed.
attestor_key.json
# Generated signed-attestation log (regenerated by `npm run demo`).
attestations.jsonl

# Hedera rail: operator key stays local. Topic id / spent-payment state is machine-local too.
.env
.hedera-state.json
*.mp4

# Питч: промежуточное — пересоздаётся make_voice.ps1 + build_video.py.
# Само VEA_pitch.mp4 НЕ игнорируем: 27.07 готовое видео исчезло с диска и с хостинга,
# а заявка на него ссылалась. Артефакт, на который кто-то смотрит, должен лежать в git.
pitch/voice/
pitch/shots/
pitch/_voice.wav
pitch/_silent.mp4
